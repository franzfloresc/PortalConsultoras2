@model Portal.Consultoras.Web.Models.OfertaFlexipagoModel
@{
    ViewBag.Title = "Administración de Ofertas Flexipago";
}
<script src="@Url.Content("~/Scripts/jquery.form.js")" type="text/javascript"></script>
<script type="text/javascript">
    paisNombre = "";
    $(document).unbind('keydown').bind('keydown', function (event) {
        var doPrevent = false;
        if (event.keyCode === 8) {
            var d = event.srcElement || event.target;
            if ((d.tagName.toUpperCase() === 'INPUT' && (d.type.toUpperCase() === 'TEXT' || d.type.toUpperCase() === 'PASSWORD'))
                 || d.tagName.toUpperCase() === 'TEXTAREA') {
                doPrevent = d.readOnly || d.disabled;
            }
            else {
                doPrevent = true;
            }
        }
        if (doPrevent) {
            event.preventDefault();
        }
    });

    jQuery(document).ready(function () {
        IniDialogConsultoraCategoria();
        IniDialogOfertaFlexipago();
        IniDialogImages();
        IniDialogLinksFlexipago();
        IniDialogStockProducto();
        IniDialogCantidadMaximaFlexipago();
        IniForm();
        fnGrilla();
        $('#txtOrdenModal').keyup(function (evt) {
            var theEvent = evt || window.event;
            var key = theEvent.keyCode || theEvent.which;
            key = String.fromCharCode(key);
            var regex = /[0-9]|\./;
            if (!regex.test(key)) {
                theEvent.returnValue = false;
                if (theEvent.preventDefault) theEvent.preventDefault();
            }
        });
        $('#txtCantidadMaxima').keyup(function (evt) {
            var theEvent = evt || window.event;
            var key = theEvent.keyCode || theEvent.which;
            key = String.fromCharCode(key);
            var regex = /[0-9]|\./;
            if (!regex.test(key)) {
                theEvent.returnValue = false;
                if (theEvent.preventDefault) theEvent.preventDefault();
            }
        });
        $('#txtOrdenModal').keypress(function (e) {
            if (e.which != 8 && e.which != 0 && (e.which < 48 || e.which > 75)) {
                return false;
            }
        });
        $('#txtCantidadMaxima').keypress(function (e) {
            if (e.which != 8 && e.which != 0 && (e.which < 48 || e.which > 57)) {
                return false;
            }
        });
        $('#txtStockMinimo').keyup(function (evt) {
            var theEvent = evt || window.event;
            var key = theEvent.keyCode || theEvent.which;
            key = String.fromCharCode(key);
            var regex = /[0-9]|\./;
            if (!regex.test(key)) {
                theEvent.returnValue = false;
                if (theEvent.preventDefault) theEvent.preventDefault();
            }
        });
        $('#txtStockMinimo').keypress(function (e) {
            if (e.which != 8 && e.which != 0 && (e.which < 48 || e.which > 57)) {
                return false;
            }
        });
        $('#txtConsultoraModal').keyup(function (evt) {
            var theEvent = evt || window.event;
            var key = theEvent.keyCode || theEvent.which;
            key = String.fromCharCode(key);
            var regex = /[0-9]|\./;
            if (!regex.test(key)) {
                theEvent.returnValue = false;
                if (theEvent.preventDefault) theEvent.preventDefault();
            }
        });
        $('#txtConsultoraModal').keypress(function (e) {
            if (e.which != 8 && e.which != 0 && (e.which < 48 || e.which > 57)) {
                return false;
            }
        });
        $('#txtPrecioNormalModal').keypress(function (evt) {
            var theEvent = evt || window.event;
            var key = theEvent.keyCode || theEvent.which;
            key = String.fromCharCode(key);
            var regex = /[0-9]|\./;
            if (!regex.test(key) || ($(this).val().indexOf('.') != -1 && key == '.')) {
                theEvent.returnValue = false;
                if (theEvent.preventDefault) theEvent.preventDefault();
            }
        });

        $("#btnBuscarConsultoraModal").click(function () {

            if ($.trim($("#txtConsultoraModal").val()) == "") {
                alert(" - Debe ingresar el código de la Consultora.");
                return false;
            }
            var items = {
                PaisID: ($("#ddlPais").val() === "" ? "0" : $("#ddlPais").val()),
                CampaniaID: ($("#ddlCampania").val() === "" ? "0" : $("#ddlCampania").val()),
                ConsultoraID: $.trim($("#txtConsultoraModal").val())
            }
            jQuery.ajax({
                type: 'POST',
                url: baseUrl + 'OfertaFlexipago/ConsultarCategoriaByConsultora',
                dataType: 'json',
                contentType: 'application/json; charset=utf-8',
                data: JSON.stringify(items),
                async: true,
                success: function (data) {
                    if (checkTimeout(data)) {
                        if (data.success == true) {
                            $("#txtCategoriaModal").text(data.Categoria);
                        }
                        else {
                            alert(data.message);
                        }
                    }
                },
                error: function (data, error) {
                    if (checkTimeout(data)) {
                        alert(data.message);
                        HideDialog("DialogRegistroOfertaFlexipago");
                    }
                }
            });
        });
        $("#btnBuscar").click(function () {
            var msjex = "";
            if ($("#ddlPais").val() == "")
                msjex += " - Debe seleccionar un País .\n";
            if ($("#ddlCampania").val() == "")
                msjex += " - Debe seleccionar un Campania .\n";

            if (msjex == "") {
                paisNombre = $("#ddlPais option:selected").text();
                paisID = $("#ddlPais").val();
                fnGrilla();
            }
            else {
                alert(msjex);
                return false;
            }
        });
        $('#ddlPais').change(function () {
            var Id = $(this).val();
            if (Id != "") {
                waitingDialog({});
                $.getJSON(baseUrl + 'OfertaFlexipago/ObtenerCampaniasYConfiguracionPorPais', { PaisID: Id == "" ? 0 : Id }, function (data) {
                    if (checkTimeout(data)) {
                        LimpiarDependencias();

                        var ddlCampania = $('#ddlCampania');
                        var ddlConfiguracionOferta = $("#ddlConfiguracionOferta");
                        ddlConfiguracionOferta.empty();
                        if (data.lista.length > 0) {
                            if (Id != "") {
                                for (var item in data.lista) {
                                    ddlCampania.append($('<option/>', {
                                        value: data.lista[item].CampaniaID,
                                        text: data.lista[item].Codigo
                                    }));
                                }
                            }
                        }
                        if (data.lstConfig.length > 0) {
                            ddlConfiguracionOferta.append($('<option/>', {
                                value: "0",
                                text: "-- Todos --"
                            }));
                            if (Id != "") {
                                for (var item in data.lstConfig) {
                                    ddlConfiguracionOferta.append($('<option/>', {
                                        value: data.lstConfig[item].CodigoOferta,
                                        text: data.lstConfig[item].Descripcion
                                    }));
                                }
                            }
                        }
                        closeWaitingDialog();
                    }
                });
            } else LimpiarDependencias();
        });
        $("#lnkBuscarCategoriaConsultora").click(function () {
            var msjex = "";
            if ($("#ddlPais").val() == "")
                msjex += " - Debe seleccionar un País .\n";
            if ($("#ddlCampania").val() == "")
                msjex += " - Debe seleccionar un Campania .\n";
            if (msjex != "") {
                alert(msjex);
                return false;
            }

            showDialog("DialogConsultoraCategoria");
            LimpiarDatosCategoriaConsultora();
            return false;
        });
        $("#lnkStock").click(function () {
            $("#hdntipoCarga").val("0");
            $('#DialogCargarStock').dialog({
                title: "Carga de CUV's de Productos por Categoria"
            });
            $("#divCategoria").html("");
            $("#divCategoria").append("<div>Formato de archivo (*.csv):</div>" +
                "<div>- Código ISO País</div>" +
                "<div>- Campaña</div>" +
                "<div>- CUV</div>" +
                "<div>- Código Categoría</div>");
            showDialog("DialogCargarStock");
            LimpiarCargaStock();
            return false;
        });
        $("#lnkCategoriaConsultora").click(function () {
            $("#hdntipoCarga").val("1");
            $('#DialogCargarStock').dialog({
                title: "Carga de Consultoras por Categoria"
            });
            $("#divCategoria").html("");
            $("#divCategoria").append("<div>Formato de archivo (*.csv):</div>" +
                "<div>- Código ISO País</div>" +
                "<div>- Campaña</div>" +
                "<div>- Código Consultora</div>" +
                "<div>- Código Categoría</div>");
            showDialog("DialogCargarStock");
            LimpiarCargaStock();
            return false;
        });
        $("#lnkModal").click(function () {
            showDialog("DialogAdministrarCantidad");
            LimpiarDatosMaximoItems();
            return false;
        });
        $("#chkImagenProducto01").click(function () {
            if ($("#chkImagenProducto01").attr('checked')) {
                $("#ImagenProducto").val($("#preview1").attr('src').replace(/^.*[\\\/]/, ''));

                var img01 = $('#ImagenProducto').val();
                if (img01.lastIndexOf("?") >= 0) {
                    img01 = img01.substring(0, img01.lastIndexOf("?"));
                    $('#ImagenProducto').val(img01);
                }

                $("#chkImagenProducto02").removeAttr('checked')
                $("#chkImagenProducto03").removeAttr('checked')
            } else
                $("#ImagenProducto").val("");
        });
        $("#chkImagenProducto02").click(function () {
            if ($("#chkImagenProducto02").attr('checked')) {
                $("#ImagenProducto").val($("#preview2").attr('src').replace(/^.*[\\\/]/, ''));

                var img01 = $('#ImagenProducto').val();
                if (img01.lastIndexOf("?") >= 0) {
                    img01 = img01.substring(0, img01.lastIndexOf("?"));
                    $('#ImagenProducto').val(img01);
                }

                $("#chkImagenProducto01").removeAttr('checked')
                $("#chkImagenProducto03").removeAttr('checked')
            } else
                $("#ImagenProducto").val("");
        });
        $("#chkImagenProducto03").click(function () {
            if ($("#chkImagenProducto03").attr('checked')) {
                $("#ImagenProducto").val($("#preview3").attr('src').replace(/^.*[\\\/]/, ''));

                var img01 = $('#ImagenProducto').val();
                if (img01.lastIndexOf("?") >= 0) {
                    img01 = img01.substring(0, img01.lastIndexOf("?"));
                    $('#ImagenProducto').val(img01);
                }

                $("#chkImagenProducto01").removeAttr('checked')
                $("#chkImagenProducto02").removeAttr('checked')
            } else
                $("#ImagenProducto").val("");
        });
        $("#lnkAdmFlexipago").click(function () {
            LimpiarLinksFlexipago();
            showDialog("DialogAdministrarLinksFlexipago");
            return false;
        });

        $("#preview1").click(function () {
            if (!($("#chkImagenProducto01").attr("disabled"))) {
                var src = $(this).attr('src');
                $("#imgModal").attr('src', src);
                showDialog('DialogImagenContainer');
            }
        });
        $("#preview2").click(function () {
            if (!($("#chkImagenProducto02").attr("disabled"))) {
                var src = $(this).attr('src');
                $("#imgModal").attr('src', src);
                showDialog('DialogImagenContainer');
            }
        });
        $("#preview3").click(function () {
            if (!($("#chkImagenProducto03").attr("disabled"))) {
                var src = $(this).attr('src');
                $("#imgModal").attr('src', src);
                showDialog('DialogImagenContainer');
            }
        });
        $('#ddlPaisCantidad').change(function () {
            var Id = $(this).val();
            if (Id != "") {
                waitingDialog({});
                $.ajaxSetup({ cache: false });
                $.getJSON(baseUrl + 'OfertaFlexipago/ObtenerCantidadMaximaPorPais', { PaisID: Id == "" ? 0 : Id }, function (data) {
                    if (checkTimeout(data)) {
                        $("#txtCantidadMaxima").val(data.Cantidad);
                        closeWaitingDialog();
                    }
                });
            } else {
                LimpiarDatosMaximoItems();
            }
        });
        $('#ddlPaisLinks').change(function () {
            var Id = $(this).val();
            if (Id != "") {
                waitingDialog({});
                $.ajaxSetup({ cache: false });
                $.getJSON(baseUrl + 'OfertaFlexipago/CargarLinkFlexipago', { PaisID: Id == "" ? 0 : Id }, function (data) {
                    if (checkTimeout(data)) {
                        $("#txtUrlFlexipago").val(data.LinksFlexipago);
                        closeWaitingDialog();
                    }
                });
            } else {
                LimpiarLinksFlexipago();
            }
        });

        $(".qq-upload-list").css("display", "none");
    });

    function RegistrarOfertaFlexipago() {
        var accion = "";
        if ($("#FlagTransaccion").val() == "1")
            accion = "UpdateOfertaFlexipago";
        else
            accion = "InsertOfertaFlexipago";

        var Item = {
            CampaniaID: jQuery('#hdCampaniaID').val(),
            CUV: jQuery('#txtCUVModal').val(),
            Descripcion: $("#txtDescripcionModal").val(),
            PrecioOferta: $("#txtPrecioOfertaModal").val(),
            PrecioNormal: $("#txtPrecioNormalModal ").val(),
            ImagenProducto: $("#ImagenProducto").val(),
            ImagenProductoAnterior: $('#ImagenProductoAnterior').val(),
            Orden: $("#txtOrdenModal").val(),
            UnidadesPermitidas: 0,
            CodigoCampania: $("#txtCampaniaModal").val(),
            FlagHabilitarProducto: $("#chkHabilitarOferta").attr('checked') ? 1 : 0,
            CodigoTipoOferta: $.trim($("#hdCodigoOferta").val()),
            FlagImagen: $("#hdFlagImagen").val(),
            PaisID: paisID
        };
        jQuery.ajax({
            type: 'POST',
            url: baseUrl + 'OfertaFlexipago/' + accion,
            dataType: 'json',
            contentType: 'application/json; charset=utf-8',
            data: JSON.stringify(Item),
            async: true,
            success: function (data) {
                if (checkTimeout(data)) {
                    if (data.success == true) {
                        alert(data.message);
                        HideDialog("DialogRegistroOfertaFlexipago");
                        LimpiarDatos();
                        jQuery("#list").setGridParam({ datatype: 'json', page: 1 }).trigger('reloadGrid');
                    }
                    else {
                        alert(data.message);
                    }
                }
            },
            error: function (data, error) {
                if (checkTimeout(data)) {
                    alert(data.message);
                    HideDialog("DialogRegistroOfertaFlexipago");
                }
            }
        });
    }

    function IniDialogConsultoraCategoria() {
        $('#DialogConsultoraCategoria').dialog({
            autoOpen: false,
            resizable: false,
            modal: true,
            closeOnEscape: true,
            width: 540,
            draggable: true,
            title: "Búsqueda de Categoría por Consultora"
        });
    }

    function IniDialogOfertaFlexipago() {
        $('#DialogRegistroOfertaFlexipago').dialog({
            autoOpen: false,
            resizable: false,
            modal: true,
            closeOnEscape: true,
            width: 800,
            draggable: true,
            title: "Registro / Edición Ofertas Flexipago",
            buttons:
            {
                "Guardar": function () {
                    var vMessage = "";
                    if (jQuery.trim($('#txtDescripcionModal').val()) == "")
                        vMessage += "- Debe ingresar la Descripción del Producto.\n";
                    if (jQuery.trim($('#txtOrdenModal').val()) == "" || jQuery.trim($('#txtOrdenModal').val()) == "0")
                        vMessage += "- Debe ingresar un Orden de Priorización válido.\n";
                    if (jQuery.trim($('#txtPrecioNormalModal').val()) == "" || jQuery.trim($('#txtPrecioNormalModal').val()) == "."
                        || jQuery.trim($('#txtPrecioNormalModal').val()) <= 0)
                        vMessage += "- Debe ingresar un Precio Normal válido.\n";
                    if (vMessage != "") {
                        alert(vMessage);
                        return false;
                    }
                    else {
                        if ($("#FlagTransaccion").val() == "1") {
                            if ($("#hdOrdenAnterior").val() != $("#txtOrdenModal").val()) {
                                $.getJSON(baseUrl + 'OfertaFlexipago/ValidarPriorizacion', { paisID: paisID, codigoOferta: $.trim($("#hdCodigoOferta").val()), CampaniaID: jQuery('#hdCampaniaID').val(), Orden: $("#txtOrdenModal").val() }, function (data) {
                                    if (data.FlagExiste == "0")
                                        RegistrarOfertaFlexipago();
                                    else {
                                        alert("El valor de Priorización ya está asignado a otro Producto, verifique");
                                        return false;
                                    }
                                });
                            } else RegistrarOfertaFlexipago();
                        } else {
                            $.getJSON(baseUrl + 'OfertaFlexipago/ValidarPriorizacion', { paisID: paisID, codigoOferta: $.trim($("#hdCodigoOferta").val()), CampaniaID: jQuery('#hdCampaniaID').val(), Orden: $("#txtOrdenModal").val() }, function (data) {
                                if (data.FlagExiste == "0")
                                    RegistrarOfertaFlexipago();
                                else {
                                    alert("El valor de Priorización ya está asignado a otro Producto, verifique");
                                    return false;
                                }
                            });
                        }
                    }
                },
                "Cancelar": function () {
                    $(this).dialog('close');
                }
            }
        });
    }

    function IsValidURL(elem) {
        var value = elem.val();
        if ($.trim(value) === "")
            return true;
        if (!/^(https?|ftp):\/\//i.test(value)) {
            value = 'http://' + value;
            elem.val(value);
        }
        return /^(https?|ftp):\/\/(((([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&amp;'\(\)\*\+,;=]|:)*@@)?(((\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])\.(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])\.(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])\.(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5]))|((([a-z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(([a-z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])*([a-z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])))\.)+(([a-z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(([a-z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])*([a-z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])))\.?)(:\d*)?)(\/((([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&amp;'\(\)\*\+,;=]|:|@@)+(\/(([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&amp;'\(\)\*\+,;=]|:|@@)*)*)?)?(\?((([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&amp;'\(\)\*\+,;=]|:|@@)|[\uE000-\uF8FF]|\/|\?)*)?(\#((([a-z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&amp;'\(\)\*\+,;=]|:|@@)|\/|\?)*)?$/i.test(elem.val());
    }

    function IniForm() {
        $('#frmCargarStock').ajaxForm({
            beforeSubmit: function () {
                waitingDialog({});
                var fileValue = $('#flStock').val();
                var extensiones = ['csv'];
                var get_ext;
                var message = '';
                //validar la extension del archivo
                get_ext = fileValue.split('.');
                get_ext = get_ext.reverse();

                if ($.inArray(get_ext[0].toLowerCase(), extensiones) == -1 && fileValue.length > 0)
                    message = message + '- El tipo de archivo es inválido, asegúrese de seleccionar con extensión (csv).'

                if (message.length > 0) {
                    closeWaitingDialog();
                    alert(message);
                    return false;
                }
            },
            success: fnRespuestaCargaStock
        });
    }

    function fnRespuestaCargaStock(responseText, statusText, xhr, $form) {
        closeWaitingDialog();
        if (checkTimeout(responseText)) {
            $("#list").setGridParam({ datatype: 'json', page: 1 }).trigger('reloadGrid');
            alert(responseText);
            $('#DialogCargarStock').dialog('close');
        }
    }

    function IniDialogImages() {
        $('#DialogImagenContainer').dialog({
            autoOpen: false,
            resizable: false,
            modal: true,
            closeOnEscape: true,
            width: 250,
            height: 270,
            title: "Oferta Flexipago",
            draggable: true
        });
    }

    function IniDialogLinksFlexipago() {
        $('#DialogAdministrarLinksFlexipago').dialog({
            autoOpen: false,
            resizable: false,
            modal: true,
            closeOnEscape: true,
            width: 480,
            draggable: true,
            title: "Links de Flexipago por País",
            buttons:
            {
                "Guardar": function () {
                    var vMessage = "";
                    if (jQuery.trim($('#ddlPaisLinks').val()) == "")
                        vMessage += "- Debe seleccionar un país.\n";
                    if (!IsValidURL($('#txtUrlFlexipago')))
                        vMessage += "- Debe ingresar una dirección web correcta.\n";
                    if (vMessage != "") {
                        alert(vMessage);
                        return false;
                    }
                    else {
                        waitingDialog({});
                        items = {
                            PaisID: $('#ddlPaisLinks').val(),
                            LinksFlexipago: $('#txtUrlFlexipago').val()
                        };
                        jQuery.ajax({
                            type: 'POST',
                            url: baseUrl + 'OfertaFlexipago/GuardarLinkFlexipago',
                            dataType: 'json',
                            contentType: 'application/json; charset=utf-8',
                            data: JSON.stringify(items),
                            async: true,
                            success: function (data) {
                                if (checkTimeout(data)) {
                                    closeWaitingDialog();
                                    if (data.success == true) {
                                        alert(data.message);
                                        HideDialog("DialogAdministrarLinksFlexipago");
                                    }
                                }
                            },
                            error: function (data, error) {
                                if (checkTimeout(data)) {
                                    closeWaitingDialog();
                                    alert(data.message);
                                    HideDialog("DialogAdministrarLinksFlexipago");
                                }
                            }
                        });
                    }
                },
                "Cancelar": function () {
                    $(this).dialog('close');
                }
            }
        });
    }

    function IniDialogStockProducto() {
        $('#DialogCargarStock').dialog({
            autoOpen: false,
            resizable: false,
            modal: true,
            closeOnEscape: true,
            width: 480,
            draggable: true,
            //title: "Carga Stock de Productos",
            buttons:
            {
                "Cargar": function () {
                    $('#frmCargarStock').submit();
                },
                "Cancelar": function () {
                    $(this).dialog('close');
                }
            }
        });
    }

    function IniDialogCantidadMaximaFlexipago() {
        $('#DialogAdministrarCantidad').dialog({
            autoOpen: false,
            resizable: false,
            modal: true,
            closeOnEscape: true,
            width: 480,
            draggable: true,
            title: "Administar Cantidad de Items a Mostrar",
            buttons:
            {
                "Guardar": function () {

                    var paisID = $("#ddlPaisCantidad").val();
                    var Cantidad = $("#txtCantidadMaxima").val();
                    var vMessage = "";
                    if (jQuery.trim(paisID) == "")
                        vMessage += "- Debe seleccionar el País.\n";
                    if (jQuery.trim(Cantidad) == "")
                        vMessage += "- Debe ingresar la Cantidad.\n";

                    if (vMessage != "") {
                        alert(vMessage);
                        return false;
                    } else
                        ActualizarMaximoItemsFlexipago(paisID, Cantidad);
                },
                "Cancelar": function () {
                    $(this).dialog('close');
                }
            }
        });
    }

    function ActualizarMaximoItemsFlexipago(paisID, cantidad) {
        jQuery.ajax({
            type: 'POST',
            url: baseUrl + 'OfertaFlexipago/ActualizarCantidadMaximaMostrar',
            dataType: 'json',
            contentType: 'application/json; charset=utf-8',
            data: JSON.stringify({ PaisID: paisID, Cantidad: cantidad }),
            async: true,
            success: function (data) {
                if (checkTimeout(data)) {
                    if (data.success == true) {
                        alert(data.message);
                        HideDialog("DialogAdministrarCantidad");
                        LimpiarDatosMaximoItems();
                    }
                    else {
                        alert(data.message);
                    }
                }
            },
            error: function (data, error) {
                if (checkTimeout(data)) {
                    alert(data.message);
                    HideDialog("DialogAdministrarCantidad");
                }
            }
        });
    }

    function LimpiarCargaStock() {
        var divcontainer = $('#flStock').closest('div');
        divcontainer.html(divcontainer.html());
    }

    function LimpiarLinksFlexipago() {
        $("#ddlPaisLinks").val("");
        $('#txtUrlFlexipago').val("");
    }

    function LimpiarDependencias() {
        var ddlCampania = $('#ddlCampania');
        ddlCampania.empty();
        ddlCampania.append($('<option/>', {
            value: "",
            text: "-- Seleccionar --"
        }));
        var ddlConfiguracionOferta = $('#ddlConfiguracionOferta');
        ddlConfiguracionOferta.empty();
        ddlConfiguracionOferta.append($('<option/>', {
            value: "",
            text: "-- Seleccionar --"
        }));
        $("#list").jqGrid("clearGridData", true).trigger("reloadGrid");
    }

    function LimpiarDatosCategoriaConsultora() {
        $("#txtConsultoraModal").val("");
        $("#txtCategoriaModal").text("");
    }

    function LimpiarDatosMaximoItems() {
        $("#ddlPaisCantidad").val("");
        $("#txtCantidadMaxima").val("");
    }

    function LimpiarDatos() {
        $('#hdCampaniaID').val("");
        $('#txtPaisModal').val("");
        $('#txtTipoOfertaModal').val("");
        $('#txtCUVModal').val("");
        $('#txtDescripcionModal').val("");
        $('#txtPrecioOfertaModal').val("");
        $('#txtPrecioNormalModal').val("");
        $('#chkHabilitarOferta').removeAttr('checked');
        $('#chkImagenProducto01').removeAttr('checked');
        $('#chkImagenProducto02').removeAttr('checked');
        $('#chkImagenProducto03').removeAttr('checked');
        $('#preview1').attr('src', '@Url.Content("~/Content/Images/prod_grilla_vacio.png")');
        $('#preview2').attr('src', '@Url.Content("~/Content/Images/prod_grilla_vacio.png")');
        $('#preview3').attr('src', '@Url.Content("~/Content/Images/prod_grilla_vacio.png")');
        $("#FlagTransaccion").val("0");
        $("#hdCodigoOferta").val("");
        $("#hdFlagImagen").val("0");
    }

    function fnGrilla() {
        jQuery("#list").jqGrid({
            url: baseUrl + "OfertaFlexipago/ConsultarOfertaFlexipago",
            hidegrid: false,
            datatype: 'json',
            postData: ({
                PaisID: function () { return $("#ddlPais").val() },
                CodigoOferta: function () { return ($("#ddlConfiguracionOferta").val() == "" ? "0" : $("#ddlConfiguracionOferta").val()); },
                CampaniaID: function () { return ($("#ddlCampania").val() == "" ? "0" : $("#ddlCampania").val()); },
                CodigoSAP: function () { return $.trim($("#txtCodigoSAP").val()); },
                CUV: function () { return $.trim($("#txtCUV").val()); },
                CategoriaID: function () { return $.trim($("#txtCategoriaID").val()); }
            }),
            mtype: 'GET',
            contentType: "application/json; charset=utf-8",
            colNames: ['Tipo Oferta', 'Código SAP', 'Campaña', 'CUV', 'Descripción', 'Precio Oferta', 'Precio Normal', 'Orden', 'Categoria', 'Stock', 'Foto', ''],
            colModel: [
                { name: 'TipoOferta', index: 'TipoOferta', width: 50, editable: true, resizable: false },
                { name: 'CodigoProducto', index: 'CodigoProducto', width: 60, editable: true, resizable: false },
                { name: 'CodigoCampania', index: 'CodigoCampania', width: 50, editable: true, resizable: false },
                { name: 'CUV', index: 'CUV', width: 40, editable: true, resizable: false },
                { name: 'Descripcion', index: 'Descripcion', width: 135, editable: true, resizable: false },
                { name: 'PrecioOferta', index: 'PrecioOferta', width: 60, editable: true, resizable: false },
                { name: 'PrecioNormal', index: 'PrecioNormal', width: 35, editable: true, resizable: false, hidden: true },
                { name: 'Orden', index: 'Orden', width: 35, editable: true, resizable: false },
                { name: 'CategoriaID', index: 'CategoriaID', width: 35, editable: true, resizable: false },
                { name: 'Stock', index: 'Stock', width: 35, editable: true, resizable: false, hidden: true },
                { name: 'Imagen', index: 'Imagen', width: 55, editable: true, resizable: false, sortable: false, align: 'center', formatter: ShowImage },
                { name: 'Options', index: 'Options', width: 35, editable: true, sortable: false, align: 'center', resizable: false, formatter: ShowActions }
            ],
            jsonReader:
            {
                root: "rows",
                page: "page",
                total: "total",
                records: "records",
                repeatitems: true,
                cell: "cell",
                id: "id"
            },
            pager: jQuery('#pager'),
            loadtext: 'Cargando datos...',
            recordtext: "{0} - {1} de {2} Registros",
            emptyrecords: 'No hay resultados',
            rowNum: 10,
            scrollOffset: 0,
            rowList: [10, 20, 30, 40, 50],
            sortname: 'Nombre',
            sortorder: 'asc',
            viewrecords: true,
            multiselect: false,
            height: 'auto',
            width: 930,
            pgtext: 'Pág: {0} de {1}',
            altRows: true,
            altclass: 'jQGridAltRowClass',
            loadComplete: function () { },
            gridComplete: function () { }
        });
        jQuery("#list").jqGrid('navGrid', "#pager", { edit: false, add: false, refresh: false, del: false, search: false });
        jQuery("#list").setGridParam({ datatype: 'json', page: 1 }).trigger('reloadGrid');

        function ShowActions(cellvalue, options, rowObject) {
            var cellValueInt = parseInt(cellvalue);
            var Edit = "&nbsp;<a href='javascript:;' onclick=\"return jQuery('#list').Editar('" + options.rowId + "','" + rowObject[3] + "','" + rowObject[11] + "','" + rowObject[14] + "','" + rowObject[15] + "','" + rowObject[16] + "','" + rowObject[13] + "','" + rowObject[17] + "','" + rowObject[18] + "','" + rowObject[9] + "','" + rowObject[6] + "');\" >" + "<img src='@Url.Content("~/Content/Images/Edit.png")' alt='Editar Oferta Flexipago' title='Editar Oferta Flexipago' border='0' /></a>";
            var Del = "&nbsp;<a href='javascript:;' onclick=\"return jQuery('#list').Eliminar('" + options.rowId + "','" + rowObject[3] + "','" + rowObject[11] + "','" + rowObject[19] + "');\" >" + "<img src='@Url.Content("~/Content/Images/NotAllowed.png")' alt='Deshabilitar Oferta Flexipago' title='Deshabilitar Oferta Flexipago' border='0' /></a>";
            if (rowObject[13] == "1")
                return Edit + Del;
            else
                return Edit;
        };

        function ShowImage(cellvalue, options, rowObject) {
            var image = "";
            var filename = rowObject[10].replace(/^.*[\\\/]/, '')
            if (rowObject[10] != "") {
                image = '<img src="' + rowObject[10] + '" alt="" width="70px" height="60px" title="" border="0" />';
            } else {
                image = '<img src="@Url.Content("~/Content/Images/prod_grilla_vacio.png")" alt="" width="60px" height="60px" title="" border="0" />';
            }
            return image;
        };
    }

    $.jgrid.extend({
        Editar: function (ID, CUV, CampaniaID, Flag, CodigoOferta, ISO, FlagCheck, FlagOrden, CodigoSAP, Stock, PrecioNormal) {
            if (ID) {
                LimpiarDatos();

                $.getJSON(baseUrl + 'OfertaFlexipago/ObtenerImagenesByCodigoSAP', { paisID: paisID, codigoSAP: CodigoSAP }, function (data) {
                    if (data.lista.length > 0) {
                        if (data.lista[0].FotoProducto01 != "") {
                            $('#preview1').attr('src',  data.lista[0].FotoProducto01);
                            $("#chkImagenProducto01").val($('#preview1').attr('src').replace(/^.*[\\\/]/, ''));

                            var img01 = $('#chkImagenProducto01').val();
                            if (img01.lastIndexOf("?") >= 0) {
                                img01 = img01.substring(0, img01.lastIndexOf("?"));
                                $('#chkImagenProducto01').val(img01);
                            }

                            $("#chkImagenProducto01").removeAttr('disabled');
                        } else
                            $("#chkImagenProducto01").attr('disabled', true);
                        if (data.lista[0].FotoProducto02 != "") {
                            $('#preview2').attr('src', data.lista[0].FotoProducto02);
                            $("#chkImagenProducto02").val($('#preview2').attr('src').replace(/^.*[\\\/]/, ''));

                            var img01 = $('#chkImagenProducto02').val();
                            if (img01.lastIndexOf("?") >= 0) {
                                img01 = img01.substring(0, img01.lastIndexOf("?"));
                                $('#chkImagenProducto02').val(img01);
                            }

                            $("#chkImagenProducto02").removeAttr('disabled');
                        } else
                            $("#chkImagenProducto02").attr('disabled', true);
                        if (data.lista[0].FotoProducto03 != "") {
                            $('#preview3').attr('src', data.lista[0].FotoProducto03);
                            $("#chkImagenProducto03").val($('#preview3').attr('src').replace(/^.*[\\\/]/, ''));

                            var img01 = $('#chkImagenProducto03').val();
                            if (img01.lastIndexOf("?") >= 0) {
                                img01 = img01.substring(0, img01.lastIndexOf("?"));
                                $('#chkImagenProducto03').val(img01);
                            }

                            $("#chkImagenProducto03").removeAttr('disabled');
                        } else
                            $("#chkImagenProducto03").attr('disabled', true);
                    } else {
                        $("#chkImagenProducto02").attr('disabled', true);
                        $("#chkImagenProducto03").attr('disabled', true);
                        $("#chkImagenProducto01").attr('disabled', true);
                    }
                    if (Flag != "0")
                        $("#FlagTransaccion").val("1");

                    $.ajaxSetup({
                        cache: false
                    });
                    if (Flag == "0") {
                        $.getJSON(baseUrl + 'OfertaFlexipago/ObtenerOrdenPriorizacion', { paisID: paisID, ConfiguracionOfertaID: FlagOrden, CampaniaID: CampaniaID }, function (data) {
                            $('#txtOrdenModal').val(data.Orden);
                        });
                    } else {
                        $('#txtOrdenModal').val(jQuery("#list").jqGrid('getCell', ID, 'Orden'));
                        $("#hdOrdenAnterior").val(jQuery("#list").jqGrid('getCell', ID, 'Orden'));
                    }

                    $('#hdCampaniaID').val(CampaniaID);
                    $("#txtPaisModal").val(paisNombre);
                    $('#txtCampaniaModal').val(jQuery("#list").jqGrid('getCell', ID, 'CodigoCampania'));

                    $('#txtTipoOfertaModal').val(jQuery("#list").jqGrid('getCell', ID, 'TipoOferta'));
                    $('#txtCUVModal').val(jQuery("#list").jqGrid('getCell', ID, 'CUV'));
                    $('#txtDescripcionModal').val(jQuery("#list").jqGrid('getCell', ID, 'Descripcion'));
                    $('#txtPrecioOfertaModal').val(jQuery("#list").jqGrid('getCell', ID, 'PrecioOferta'));
                    $('#txtPrecioNormalModal').val(PrecioNormal);

                    var imagen = $($("#list").jqGrid('getCell', ID, 'Imagen')).attr('src').replace(/^.*[\\\/]/, '');

                    if (imagen != "prod_grilla_vacio.png") {
                        $('#ImagenProductoAnterior').val(imagen);

                        var img01 = $('#ImagenProductoAnterior').val();
                        if (img01.lastIndexOf("?") >= 0) {
                            img01 = img01.substring(0, img01.lastIndexOf("?"));
                            $('#ImagenProductoAnterior').val(img01);
                        }

                        $('#ImagenProducto').val(imagen);

                        img01 = $('#ImagenProducto').val();
                        if (img01.lastIndexOf("?") >= 0) {
                            img01 = img01.substring(0, img01.lastIndexOf("?"));
                            $('#ImagenProducto').val(img01);
                        }


                    } else {
                        $('#ImagenProductoAnterior').val("");
                        $('#ImagenProducto').val("");
                    }

                    var img01 = $($("#list").jqGrid('getCell', ID, 'Imagen')).attr('src').replace(/^.*[\\\/]/, '');
                    if (img01.lastIndexOf("?") >= 0) {
                        img01 = img01.substring(0, img01.lastIndexOf("?"));
                        $('#ImagenProductoAnterior').val(img01);
                    }

                    if ($("#chkImagenProducto03").val() == img01)
                        $('#chkImagenProducto03').attr('checked', true);
                    if ($("#chkImagenProducto02").val() == img01)
                        $('#chkImagenProducto02').attr('checked', true);
                    if ($("#chkImagenProducto01").val() == img01)
                        $('#chkImagenProducto01').attr('checked', true);
                    if (FlagCheck == 1)
                        $('#chkHabilitarOferta').attr('checked', true);
                    else
                        $('#chkHabilitarOferta').removeAttr('checked');
                    $('#hdCodigoOferta').val(CodigoOferta);

                    showDialog("DialogRegistroOfertaFlexipago");
                });

            } else {
                alert("Debe seleccionar un Producto, verifique");
            }
            return false;
        },
        Eliminar: function (ID, CUV, CampaniaID, TipoOfertaSisID) {
            var elimina = confirm('¿ Está seguro que desea deshabiltar el producto seleccionado?');
            if (!elimina)
                return false;

            var Item = {
                CampaniaID: CampaniaID,
                CUV: CUV,
                PaisID: paisID,
                TipoOfertaSisID: TipoOfertaSisID
            };

            waitingDialog({});
            jQuery.ajax({
                type: 'POST',
                url: baseUrl + 'OfertaFlexipago/DeshabilitarOfertaFlexipago',
                dataType: 'json',
                contentType: 'application/json; charset=utf-8',
                data: JSON.stringify(Item),
                async: true,
                success: function (data) {
                    if (checkTimeout(data)) {
                        closeWaitingDialog();
                        LimpiarDatos();
                        if (data.success == true) {
                            alert(data.message);
                            jQuery("#list").setGridParam({ datatype: 'json', page: 1 }).trigger('reloadGrid');
                        }
                        else
                            alert(data.message);
                    }
                },
                error: function (data, error) {
                    if (checkTimeout(data)) {
                        closeWaitingDialog();
                        alert("ERROR");
                    }
                }
            });
            return false;
        }
    });
</script>

<div class="wrap_cab">
    <div class="filtros">
        <div class="elements">
            <div class="div-2">
                <h1>Ofertas <span>Flexipago</span></h1>
            </div>
            <div class="div-3">
                <div class="titC">País:</div>
                <div class="selectP2 borde_redondeado">
                    @Html.DropDownListFor(model => model.CampaniaID, new SelectList(Model.lstPais, "PaisID", "Nombre"), "-- Seleccionar --", new { id = "ddlPais" })
                </div>
                <div class="titB">Campaña:</div>
                <div class="selectP2 borde_redondeado">
                    @Html.DropDownListFor(model => model.CampaniaID, new SelectList(Model.lstCampania, "CampaniaID", "NombreCorto"), "-- Seleccionar --", new { id = "ddlCampania" })
                </div>
                <div class="input_search">
                    <a href="#" id="lnkAdmFlexipago">
                        <img title="Cargar Link de Flexipago" src='@Url.Content("~/Content/Images/flexipago_url.png")' alt="" />
                    </a>
                    <a href="#" id="lnkStock">
                        <img title="Cargar CUV's por Categoria" src='@Url.Content("~/Content/Images/stok.png")' alt="" />
                    </a>
                    <a href="#" id="lnkCategoriaConsultora">
                        <img title="Cargar Consultora por Categoría" src='@Url.Content("~/Content/Images/cargar_consultora.png")' alt="" />
                    </a>
                    <a href="#" id="lnkModal">
                        <img title="Cargar Items a Mostrar" src='@Url.Content("~/Content/Images/admmaxitems.png")' alt="" />
                    </a>
                </div>
            </div>
            <div class="div-3">
                <div class="titC">Tipo Oferta:</div>
                <div class="selectP2 borde_redondeado">
                    @Html.DropDownListFor(model => model.CodigoTipoOferta, new SelectList(Model.lstConfiguracionOferta, "CodigoOferta", "Descripcion"), "-- Seleccionar --", new { id = "ddlConfiguracionOferta" })
                </div>
                <div class="titB">Categoría:</div>
                <div class="selectP2 borde_redondeado">
                    @Html.TextBoxFor(model => model.CategoriaID, new { id = "txtCategoriaID", maxlength = "20" })
                </div>
                <div class="input_search">
                    <a href="#" id="lnkBuscarCategoriaConsultora">
                        <img title="Buscar Categoría por Consultora" src='@Url.Content("~/Content/Images/search_cat_consul.png")' alt="" />
                    </a>
                </div>

            </div>
            <div class="div-3">
                <div class="titC">Código SAP:</div>
                <div class="selectP2 borde_redondeado">
                    @Html.TextBoxFor(model => model.CodigoSAP, new { id = "txtCodigoSAP", maxlength = "20" })
                </div>
                <div class="titB">CUV:</div>
                <div class="selectP2 borde_redondeado">
                    @Html.TextBoxFor(model => model.CUV, new { id = "txtCUV", maxlength = "6" })
                </div>

            </div>
            <div class="div-3">
                <div class="titC"></div>
                <div class="input_search">
                    <input type="button" value="Buscar" id="btnBuscar" name="buscar">
                </div>
            </div>
        </div>
    </div>

    <div class="wrap">
        <div class="container clearfix">
            <div class="border-wrapper">
                <table id="list"></table>
                <div id="pager"></div>
            </div>
        </div>
    </div>

    <div id="loadingScreen"></div>

    <div id="DialogRegistroOfertaFlexipago" style="display: none;">
        <input type="hidden" id="FlagTransaccion" value="0" />
        <input type="hidden" id="hdOrdenAnterior" value="" />
        <input type="hidden" id="hdCampaniaID" />
        <input type="hidden" id="ImagenProducto" />
        <input type="hidden" id="ImagenProductoAnterior" />
        <input type="hidden" id="hdCodigoOferta" value="" />
        <input type="hidden" id="hdFlagImagen" value="0" />
        <div class="Content_modal">
            <div class="div-3">
                <div class="titG">País:</div>
                <div class="selectP2 disabled borde_redondeado">
                    <input type="text" id="txtPaisModal" readonly="readonly" />
                </div>
                <div class="titG">Campaña:</div>
                <div class="selectP2 disabled borde_redondeado">
                    <input type="text" id="txtCampaniaModal" readonly="readonly" />
                </div>
            </div>
            <div class="div-3">
                <div class="titG">Tipo Oferta:</div>
                <div class="selectP2 disabled borde_redondeado">
                    <input type="text" id="txtTipoOfertaModal" readonly="readonly" />
                </div>
                <div class="titG">CUV:</div>
                <div class="selectP2 disabled borde_redondeado">
                    <input type="text" id="txtCUVModal" readonly="readonly" />
                </div>
            </div>
            <div class="div-3">
                <div class="titG">Descripción:</div>
                <div class="selectP2 borde_redondeado">
                    <input type="text" id="txtDescripcionModal" />
                </div>
                <div class="titG">Precio Oferta:</div>
                <div class="selectP2 disabled borde_redondeado">
                    <input type="text" id="txtPrecioOfertaModal" readonly="readonly" />
                </div>
            </div>
            <div class="div-3">
                <div class="titG">Precio Normal:</div>
                <div class="selectP2 borde_redondeado">
                    <input type="text" id="txtPrecioNormalModal" maxlength="7"/>
                </div>
                <div class="titG">Orden:</div>
                <div class="selectP2 borde_redondeado">
                    <input type="text" id="txtOrdenModal" maxlength="6">
                </div>
            </div>
            <div class="div-3">
                <div class="titG">Imagen:</div>
                <div class="pdr_modal borde_redondeado aligncenter">
                    <img src='@Url.Content("~/Content/Images/prod_grilla_vacio.png")' id="preview1" style="cursor:pointer;">
                </div>
                <div class="pdr_modal borde_redondeado aligncenter">
                    <img src='@Url.Content("~/Content/Images/prod_grilla_vacio.png")' id="preview2" style="cursor:pointer;">
                </div>
                <div class="pdr_modal borde_redondeado aligncenter">
                    <img src='@Url.Content("~/Content/Images/prod_grilla_vacio.png")' id="preview3" style="cursor:pointer;">
                </div>
                <div class="titG">¿Habilitar Oferta?</div>
                <div class="selectCheck borde_redondeado">
                    <input type="checkbox" class="check" id="chkHabilitarOferta" />
                </div>
            </div>
            <div class="div-3">
                <div class="titG"></div>
                <div class="pdr_modal_check aligncenter">
                    <input type="checkbox" id="chkImagenProducto01">
                </div>
                <div class="pdr_modal_check aligncenter">
                    <input type="checkbox" id="chkImagenProducto02">
                </div>
                <div class="pdr_modal_check aligncenter">
                    <input type="checkbox" id="chkImagenProducto03">
                </div>
            </div>
        </div>
    </div>

    <div id="DialogAdministrarLinksFlexipago" style="display: none;">
        <div class="Content_modal">
            <div class="div-3">
                <div class="titC">
                    País :
                </div>
                <div class="selectA borde_redondeado">
                    @Html.DropDownListFor(model => model.CampaniaID, new SelectList(Model.lstPais, "PaisID", "Nombre"), "-- Seleccionar --", new { id = "ddlPaisLinks" })
                </div>
            </div>
            <div class="div-3">
                <div class="titC">
                    Link :
                </div>
                <div class="selectA borde_redondeado">
                    <input type="text" id="txtUrlFlexipago" name="txtUrlFlexipago" />
                </div>
            </div>
        </div>
    </div>

    <div id="DialogCargarStock" style="display: none;">
        @using (Html.BeginForm("ActualizarStockMasivo", "OfertaFlexipago", FormMethod.Post, new { id = "frmCargarStock", enctype = "multipart/form-data" }))
        {
            <div class="Content_modal">
                <input type="hidden" id="hdntipoCarga" name="tipoCarga" value="0" />
                <div class="div-3">
                    <div class="titC">
                        Archivo :
                    </div>
                    <div class="selectA borde_redondeado">
                        <input type="file" id="flStock" name="flStock" />
                    </div>
                </div>
            </div>
            <div class="div-3">
                <div class="titC">
                </div>
                <div id="divCategoria" class="titAuto" style="text-align: left;">
                </div>
            </div>
        }
    </div>

    <div id="DialogAdministrarCantidad" style="display: none;">
        <div class="Content_modal">
            <div class="div-3">
                <div class="titC">
                    País :
                </div>
                <div class="selectA borde_redondeado">
                    @Html.DropDownListFor(model => model.CampaniaID, new SelectList(Model.lstPais, "PaisID", "Nombre"), "-- Seleccionar --", new { id = "ddlPaisCantidad" })
                </div>
            </div>
            <div class="div-3">
                <div class="titC">
                    Cantidad :
                </div>
                <div class="selectA borde_redondeado">
                    <input type="text" id="txtCantidadMaxima" name="txtCantidadMaxima" maxlength="3" />
                </div>
            </div>
        </div>
    </div>

    <div id="DialogImagenContainer" style="display: none;">
        <div class="Content_modal">
            <img src='' id="imgModal" />
        </div>
    </div>
    <div id="DialogConsultoraCategoria" style="display: none;">
        <div class="Content_modal">
            <div class="div-3">
                <div class="titC">
                    Consultora :
                </div>
                <div class="selectA borde_redondeado">
                    <input type="text" id="txtConsultoraModal" name="txtConsultoraModal" maxlength="20" />
                </div>
                <div class="input_search">
                    <input type="button" value="Buscar" id="btnBuscarConsultoraModal" name="buscar">
                </div>
            </div>
            <div class="div-3">
                <div class="titC">
                    Categoría :
                </div>
                <div class="titAuto">
                    <span id="txtCategoriaModal"></span>
                </div>
            </div>
        </div>
    </div>
</div>